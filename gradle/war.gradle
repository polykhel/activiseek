apply plugin: "war"

bootWar {
    mainClassName = "com.activiseek.api.ActiviseekApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
